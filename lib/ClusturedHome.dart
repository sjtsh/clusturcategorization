import 'package:flutter/material.dart';

class ClusturedHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Container(color: Colors.red,),
      ),
    );
  }
}
